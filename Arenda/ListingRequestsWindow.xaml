<Window x:Class="Arenda.Windows.ListingRequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заявления на объявления"
        Width="1000"
        Height="700"
        WindowStartupLocation="CenterScreen"
        Background="#F9FAFB">
    <Grid>
        <Border Background="White" CornerRadius="10" Padding="15" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Верхняя панель с кнопкой назад -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="← Назад"
                            Width="90"
                            Height="32"
                            Margin="0,0,12,0"
                            HorizontalAlignment="Left"
                            Click="BackButton_Click"
                            Background="#F1F5F9"
                            Foreground="#1E90FF"
                            BorderThickness="1"
                            BorderBrush="#1E90FF"
                            FontWeight="Bold"
                            FontSize="13">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="7"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="2">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <TextBlock Text="Заявления на объявления"
                               Grid.Column="1"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#1E90FF"/>
                </Grid>

                <!-- Список заявлений -->
                <ListView x:Name="RequestsListView" Grid.Row="1" Margin="10" SelectionChanged="RequestsListView_SelectionChanged"
                          VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel VerticalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FFFFFF" Margin="5" Padding="10" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding OwnerName}" FontWeight="Bold" FontSize="14" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Address}" FontSize="12" Foreground="Gray" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding City}" FontSize="12" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="3" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding SubmissionDate, StringFormat='{}{0:dd.MM.yyyy}'}" FontWeight="Bold" FontSize="13" Foreground="#1E90FF"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="4" Margin="0,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Status}" FontSize="12" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Window>